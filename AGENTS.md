# Viewer 项目 — Agent 说明（导入 / 指纹 / 测试）

本文约束本仓库中与「数据集路径导入、重复识别、能力测验」相关的实现方式：**低耦合、高模块化**；具体测试资产与性能约定放在仓库根目录下的 **`cs/`** 文件夹中（**文件与文档优先使用中文命名**）。

---

## 1. 架构原则

- **低耦合**：路由、数据库、ingest 适配器、指纹算法、根路径解析之间不得互相内联实现细节；只通过**窄接口**（纯数据、小函数、可注入的 lookup）协作。
- **高模块化**：下列能力各自为**独立模块**（独立目录或包，边界清晰），禁止合并成「万能 import_jobs 大文件」式的杂糅逻辑（编排可以薄，但算法与测试必须外置）。

---

## 2. 后端模块划分（职责与禁止项）

| 模块（建议包路径） | 职责 | 禁止 |
|-------------------|------|------|
| **指纹模块**（如 `back/app/fingerprint/` 或等价名） | 实现与 `MD5-demo/fast_metadata_hash.py` 一致的**元数据指纹**：递归 `scandir`（不跟随符号链接）、每文件 `相对路径|size|mtime`、排序、`MD5(utf-8 文本)`；返回 `(fingerprint_hex, file_count)` 及可选耗时统计。**不读文件内容**。 | 依赖 FastAPI、SQLAlchemy、ingest；不发起导入。 |
| **路径与根解析模块**（`back/app/dataset_ingest_root/`） | 将用户选择的**可能多层嵌套**的目录 `resolve()` 后，解析出 **ingest 根路径**（唯一 TopPIC 树根）。 | 实现指纹算法；不直接写业务大表。 |
| **路径导入编排**（如保留在 `import_jobs` 但仅作**薄编排**） | 校验策略、job 阶段、调用 planner / `ingest_universal_*`、finalize、写指纹列与 `source_root`、冲突错误信息。 | 在编排层手写递归扫盘与 MD5；不把测试代码放此处。 |

---

## 3. 重复识别与性能

- **重复识别**以指纹模块输出的 **32 位小写 hex** 为唯一业务键（列名可与 `AGENTS` 同步，如 `source_dataset_fingerprint`，注释标明为**元数据 manifest 的 MD5**，避免与「全文件内容哈希」混淆）。
- **性能目标**：在已解析的 **ingest 根目录** 上，指纹计算墙钟时间 **≤ 0.5s**（以基准数据集实测为准，见 `cs/` 下约定）。
- **基准数据**：`MZ20160222DS_histone49_html`；性能验收在 `cs/指纹性能测验.py` 顶部改 `BENCHMARK_DATASET_PATH` 直接运行；也可用 `VIEWER_BENCH_DATASET_ROOT` 覆盖。**主业务代码**中禁止写死本机盘符。

---

## 4. 测试模块目录：`cs/`

- **`cs/`** 专用于**能力测验 / 性能验收 / 与主业务低耦合的说明文档**；不替代 `back/tests/` 中的 pytest 单元测试，但**性能与基准相关**的脚本、数据说明、验收步骤应放在 `cs/` 并由中文文件名组织，便于人工查阅与单独执行。
- **命名**：`cs/` 内新建 Markdown、脚本、子目录时**优先使用中文文件名**（例如 `性能测验约定.md`、`指纹性能入口.py`）。若脚本需作为可导入包被 pytest 收集，可再设一层英文包名，但用户可见文档仍以中文为主。
- **依赖**：`cs/` 内脚本应主要通过「调用指纹模块的公开 API」完成测验，避免复制粘贴 demo 中的算法实现。

---

## 5. 对 Agent 的实施提示

- 修改导入链路时：先查 `AGENTS.md` 与 `cs/` 下约定，再改代码。
- 新增与「指纹 / 根路径 / 性能」相关的需求时：优先扩展**对应独立模块**的接口，而不是在 `api/v1/imports.py` 内膨胀。
- 交付前：**跑通** `cs/` 中约定的性能测验步骤（或等价 pytest 标记），并记录是否满足 0.5s 目标。

---

## 6. 与旧实现的取舍

- **移除**：ZIP 上传、临时解压、`source_zip_sha256` 及 ZIP 专用查重。
- **列**：`datasets.source_dataset_fingerprint`（CHAR(32) 元数据 manifest MD5，部分 UNIQUE）。
- **保留** ingest 与 planner 的既有能力；路径导入在解析出 ingest 根后与其对接。
