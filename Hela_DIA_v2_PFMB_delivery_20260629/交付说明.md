# Hela DIA v2 · PFMB 交付包

**给**：泽旭（DIA 项目前端/接入修改）
**版本**：v2（frag.RT 每列一条，834,455 条 PRSM）
**打包时间**：2026-06-29 10:51

## 必看文件

| 路径 | 说明 |
|------|------|
| `data/results.pfmb` | 主结果（峰–碎片匹配） |
| `data/index.json` | 前端主索引 items[] |
| `data/prsm.v2.mapping.jsonl` | 同 index（JSONL 流式） |
| `data/prsm.v2.manifest.json` | 元信息 |
| `docs/前端_v2_接入说明.md` | **接入必读** |
| `pfmb_io.py` | Python 读 PFMB |

## 读取示例

```python
from pfmb_io import PfmbReader
with PfmbReader("data/results.pfmb") as r:
    rec = r.read_by_prsm_index(12345)
    print(rec.peptide, len(rec.matches))
```

## v2 要点

- 1 条 pkl 行 → N 条 PFMB（N = frag.RT 列数）
- 键：`prsm_index`, `source_row`, `slot_index`, `slot_rt`
- 热力图矩阵仍从 pos.pkl 的 frag.chrom 读，用 source_row + slot_index 对齐

复跑：`powershell -File pipelines/run_bottom_up_hela_v2.ps1`
