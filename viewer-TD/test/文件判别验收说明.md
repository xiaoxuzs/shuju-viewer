# 文件判别模块验收（viewer-TD/test）

验证 `back/app/toppic_admission/` 的形态判定、run 配对与**英文拒绝说明**。

测试数据：`test/xzx_PXD045330/`（形态 B：topfd + toppic + xml + msalign + mzML）

---

## 一键验收

```powershell
cd e:\viewer\viewer-TD\test
python 文件判别验收.py
```

| 步骤 | 内容 |
|------|------|
| pytest | `test_toppic_admission_*` + `test_dataset_ingest_root` |
| 真实 Form B | `xzx_PXD045330` → `need_pfmb`，1 组 triple，三文件路径存在 |
| 拒绝（仅 mzML） | 临时目录，英文 `only_mzml` |
| 拒绝（无 mzML） | 复制 330 数据并去掉 mzML，英文 `missing_mzml` |

退出码 **0** = 通过。

---

## 与单元测试的关系

| 位置 | 用途 |
|------|------|
| `back/tests/test_toppic_admission_*.py` | CI / 开发：tmp_path 合成布局 |
| `test/文件判别验收.py` | 人工 / 交付：真实 330 样本 + pytest 汇总 |

与 `test/pfmb_验收.py` 分离：本脚本**不**调用 `pfmb_bridge.exe`。

---

## 预期结果（xzx_PXD045330）

| 字段 | 值 |
|------|-----|
| `route` | `need_pfmb` |
| `run_triples` | 1 |
| `run_key` | `20191118_rvg262_lt_110516-13_1000-1100_techrep01` |
| PrSM XML | `..._ms2_toppic_prsm.xml` |
| MS2 msalign | `..._ms2.msalign` |
| mzML | `20191118_rvg262_LT_110516-13_1000-1100_Techrep01.mzML` |
