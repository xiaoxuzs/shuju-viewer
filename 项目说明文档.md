# 项目说明文档（`viewer`）

本文档面向**维护者/二次开发者**，解释仓库根目录下 `back/`、`front/`、`shuju/`、`docs/` 各自是什么、目前有哪些接口/数据流、兼容哪些输入数据形态，以及后续新增功能/数据格式时推荐怎么做。

> 重要：本项目的“单一真值”是 `docs/universal_schema.sql`（universal schema）。后端读路径与写路径都围绕它运转，且读路径使用 raw SQL（`sqlalchemy.text`），不是 ORM。

---

## 1. 仓库目录总览

- **`back/`**：FastAPI 后端（读 API + 导入任务 + 解析/缓存谱图）。
  - **职责**：
    - 对外提供 REST API（`/api/v1/...`）供前端浏览数据集、蛋白、proteoform、PrSM 以及谱图。
    - 支持从 ZIP 上传导入数据集（后台线程跑任务，进度写入 `import_jobs` 表）。
    - 根据数据集能力（`datasets.capabilities.spectra_source`）选择谱图来源：
      - `topfd_js`：从磁盘读取 TopFD `spectrum*.js`
      - `mzml_memory`：按 run_id 懒加载 mzML 到内存，并按 scan 取谱

- **`front/`**：React + Vite 前端（浏览器 UI）。
  - **职责**：
    - 提供数据集列表、cutoff 卡片、Protein/Proteoform/PrSM 列表与详情页。
    - 通过 `src/api/client.ts` 调用后端 API。
    - `PrsmDetailPage` 将 PrSM 详情 JSON（`annotated_protein` / `ms_peaks`）解析为 UI 需要的结构，联动 MS1/MS2 谱图展示、matched peak 表格、fragmentation 视图等。

- **`shuju/`**：本地数据目录（默认数据根目录）。
  - **定位**：项目运行时会把导入/解压的数据集放在这里（也可通过 `back/.env` 的 `DATA_ROOT` 指向其它路径）。
  - **内容形态**：
    - TopPIC/TopFD HTML 输出目录（包含 `topfd/`、`toppic_*_cutoff/` 等）
    - 大体量的 `topfd/ms1_json/spectrum*.js`、`topfd/ms2_json/spectrum*.js`
    - 示例 ZIP 包（用于 UI 上传导入）
    - 解压/辅助脚本（例如 `shuju/jieya.py` 并发解压实验脚本）

- **`docs/`**：项目规范与部署/架构文档。
  - **关键文档**：
    - `docs/architecture.md`：当前实现的架构地图（读/写模型、任务流、命名规则、已知二期项）。
    - `docs/universal_schema.sql`：数据库表结构“唯一真值”。
    - `docs/解释文件需求.md`：`prsm.js` 必需字段规范（当前 TopFD JS 版 + mzML 内存版建议）。
    - `docs/添加数据说明.md`：如何导入一套新的 TopPIC/TopFD 结果（CLI 与目录结构要求）。
    - `docs/dataset-ir-v0.md`：更抽象的“数据准入/统一契约（IR）”设计草案（未来多格式接入方向）。

---

## 2. 后端（`back/`）概览

### 2.1 运行入口与启动时初始化

- `back/app/main.py`
  - FastAPI app，注册 CORS 与路由。
  - 启动时（lifespan）会尝试自动补齐一些 DB schema：
    - `import_jobs` 表（用于 UI 导入进度轮询）
    - `datasets.source_zip_sha256`（用于“同 ZIP 重复导入”冲突检测）
    - `runs.run_metadata`（用于 run ↔ mzML 路径的严格映射）

### 2.2 API 路由聚合

- `back/app/api/v1/__init__.py`
  - 统一挂在 `/api/v1`
  - 当前包含模块：
    - `datasets` / `imports` / `proteins` / `proteoforms` / `prsms` / `spectra` / `mzml_spectra`

---

## 3. 后端 API 接口清单（当前实现）

> 约定：下面所有接口都在 `/api/v1` 前缀下。

### 3.1 Meta

- **GET** `/health`
  - **用途**：健康检查
  - **返回**：`{ "status": "ok" }`

### 3.2 Datasets（数据集与 cutoff 统计）

实现：`back/app/api/v1/datasets.py`

- **GET** `/datasets`
  - **用途**：数据集列表（每个数据集附带两个“虚拟 cutoff”的统计）
  - **返回**：`DatasetOut[]`

- **GET** `/datasets/{slug}`
  - **用途**：数据集详情（同上）
  - **返回**：`DatasetOut`

- **DELETE** `/datasets/{slug}`
  - **用途**：删除数据集（DB cascade + 磁盘目录清理）
  - **关键约束**：
    - 若该 `slug` 存在 `queued/running` 的导入任务，拒绝删除（409）
    - 只允许删除 `DATA_ROOT` 子树下的目录（安全护栏）
  - **返回**：`DatasetDeletedOut`

#### cutoff 的说明（“虚拟 cutoff”）

universal schema **没有** `cutoffs` 表；cutoff 信息来自：

- `identification_matches.extra_metadata.source_cutoff`：字符串 `"prsm"` / `"proteoform"`
- `back/app/api/v1/universal_compat.py`：
  - `cutoff_kinds()` 固定顺序：`("prsm", "proteoform")`
  - `cutoff_id(kind)`：合成稳定整数 id（1/2，前端依赖，勿改）
  - `cutoff_label(kind)`：展示文案

数据集卡片里的统计数字（protein/proteoform/prsm）由 `datasets.py` 的 SQL 聚合计算，严格按 `source_cutoff` 过滤得到。

### 3.3 Imports（ZIP 上传导入）

实现：`back/app/api/v1/imports.py`

- **POST** `/imports`
  - **用途**：上传 ZIP 并启动后台导入任务
  - **入参**（multipart form）：
    - `file`: `.zip`
    - `slug`: 数据集唯一 slug
    - `name`: 展示名
    - `description`: 可选
  - **行为**：
    - 上传流式落盘到 temp 文件
    - 计算 ZIP sha256，与 `datasets.source_zip_sha256` 做重复导入检测（冲突 409）
    - 插入 `import_jobs`（queued）
    - 启动后台线程执行导入（见 4 章）
  - **返回**：`202 Accepted` + `ImportJobCreatedOut`

- **GET** `/imports/{job_id}`
  - **用途**：轮询导入任务进度
  - **返回**：`ImportJobOut`

### 3.4 Proteins（按 cutoff 过滤后的 protein 列表/详情）

实现：`back/app/api/v1/proteins.py`

- **GET** `/datasets/{slug}/cutoffs/{cutoff}/proteins`
  - **Query**：
    - `page`、`page_size`
    - `search`（对 protein 名称/描述 ILIKE）
    - `sort`、`order`
  - **cutoff 过滤策略**：
    - protein 本身在 universal schema 中是“跨 cutoff 共享”的行
    - 这里通过 `EXISTS identification_matches WHERE source_cutoff = :cutoff` 走关联链过滤，确保列表确实对应当前 cutoff 的可见集合

- **GET** `/datasets/{slug}/cutoffs/{cutoff}/proteins/{protein_id}`
  - `protein_id` 为数据库主键（`proteins.protein_id`）
  - 返回 protein 详情 + 下属 proteoform 列表（同样按 cutoff 过滤）

### 3.5 Proteoforms（按 cutoff 过滤后的 proteoform 列表/详情）

实现：`back/app/api/v1/proteoforms.py`

- **GET** `/datasets/{slug}/cutoffs/{cutoff}/proteoforms`
  - **Query**：
    - `page`、`page_size`
    - `protein_id`（可选，仅看某 protein 下形式）
    - `sort`、`order`
  - **cutoff 过滤策略**：
    - 通过 `EXISTS identification_matches ... source_cutoff = :cutoff` 判断该 proteoform 在该 cutoff 下“出现过鉴定”，否则不显示

- **GET** `/datasets/{slug}/cutoffs/{cutoff}/proteoforms/{proteoform_id}`
  - `proteoform_id` 为数据库主键（`proteoforms.proteoform_id`）
  - 返回 proteoform 详情 + PrSM 列表（按 cutoff 过滤）

### 3.6 PrSMs（按 cutoff 的 PrSM 列表/详情）

实现：`back/app/api/v1/prsms.py`

- **GET** `/datasets/{slug}/cutoffs/{cutoff}/prsms`
  - **Query**：
    - `page`、`page_size`
    - `protein_id`、`proteoform_id`（可选过滤）
    - `sort`、`order`

- **GET** `/datasets/{slug}/cutoffs/{cutoff}/prsms/{prsm_id}`
  - **重要**：这里的 `prsm_id` 是 **TopPIC 业务编号**（不是 DB 自增主键）
  - 在 universal schema 中，PrSM 存在 `identification_matches`：
    - cutoff：`extra_metadata.source_cutoff`
    - 业务 prsm id：`extra_metadata.source_prsm_id`
    - `detail_path` 指向磁盘 `prsm*.js` 文件，详情页按需读取解析（`universal_compat.load_prsm_detail`）
  - 返回 `PrsmDetailOut`，包含：
    - `ms_header`、`ms_peaks`、`annotated_protein` 三个大 JSON（来自磁盘 `prsm*.js`）

### 3.7 Spectra（TopFD JS 磁盘谱图）

实现：`back/app/api/v1/spectra.py` + `back/app/services/spectrum_cache.py`

- **GET** `/datasets/{slug}/spectra/ms1/{spec_id}`
- **GET** `/datasets/{slug}/spectra/ms2/{spec_id}`

解析顺序：

1. 优先用 DB 里的 `datasets.source_root`（导入时写入的绝对路径）
2. 如果路径不可用，则 fallback 到 `DATA_ROOT/<slug_dir>`（保证把 `shuju/` 拷到别的机器/盘符后仍能跑）

### 3.8 mzML Spectra（mzML 内存懒加载谱图）

实现：`back/app/api/v1/mzml_spectra.py` + `back/app/services/mzml_store.py`

- **GET** `/datasets/{dataset_id}/runs/{run_id}/spectra/{scan_number}`
  - 根据 `runs.run_metadata.mzml_file_path` 找 mzML 文件
  - 首次请求会把该 run 的 mzML **整文件载入内存并建立 scan 索引**（进程级缓存，LRU 驱逐）
  - 返回的谱图形状为后端内部抽取结果（`mzml_store._extract_spectrum`）：
    - `scan`、`native_id`、`ms_level`、`rt_seconds`
    - `mz[]`、`intensity[]`
    - `precursor`（若 MS2）

> 注意：这个接口只在数据集 `capabilities.spectra_source == "mzml_memory"` 时由前端使用。

---

## 4. 导入与数据兼容（`back/app/services/import_jobs.py`）

### 4.1 导入任务的持久化与进度

- 任务表：`import_jobs`
- 进度字段：
  - `status`: `queued | running | success | failed`
  - `progress`: 0..100
  - `stage`: 阶段码（`extract/init/proteins/matches/finalize/...`）
  - `stage_label`: 中文阶段名（前端直接显示）
  - `stage_detail`: 细节行（例如 “123/456 files extracted”）

### 4.2 ZIP 包兼容的两种“输入形态”

后台任务解压后会自动识别：

#### A) TopPIC HTML 输出树（推荐/默认）

判定条件（其一存在即可）：

- `toppic_prsm_cutoff/data_js/proteins.js` 存在（关键文件）

导入路径：

- 使用 `app.ingest.universal_toppic_adapter.ingest_universal_toppic(...)`
- 当谱图源为 TopFD JS 时导入会走更快路径（当前实现会选择 `mode="fast"`，避免逐个读取所有 `prsm*.js`）

#### B) `prsm*.js` bundle（仅 PrSM 详情 + mzML）

判定条件：

- `data/` 目录存在，且其中存在 `data/prsm*.js`

导入路径：

- 必须走 mzML 内存模式（因为没有 TopFD 的 `spectrum*.js`）
- 使用 `app.ingest.universal_prsm_js_adapter.ingest_universal_prsm_js(...)`

### 4.3 谱图来源的自动判定（TopFD JS vs mzML memory）

解压后若检测不到两类 TopFD 目录都齐全：

- `topfd/ms1_json/spectrum*.js`
- `topfd/ms2_json/spectrum*.js`

则会切换为：

- `spectra_source = "mzml_memory"`
- 并在导入期执行 **strict mzML mapping 校验**（只做文件发现与一一对应匹配，不读 mzML 内容）：
  - 从 `prsm*.js` 提取 `ms.ms_header.spectrum_file_name`
  - 在解压目录内收集 `*.mzML/*.mzml[.gz]`
  - 通过规范化规则做 1:1 映射（冲突或缺失即导入失败）

导入成功后会写入：

- `datasets.capabilities.spectra_source`：`"topfd_js"` 或 `"mzml_memory"`
- 对于 `"mzml_memory"`：
  - 会将 `runs.run_metadata.mzml_file_path` 写入数据库（run ↔ mzML 路径严格绑定）

### 4.4 解压的实现与安全性

解压要点：

- **zip-slip 防护**：拒绝绝对路径与 `..` 越界条目。
- **先建目录树，再多线程写文件**（固定线程池大小、每线程独立 `ZipFile` 句柄）。

### 4.5 原子替换（`.incoming`）

解压目录使用：

- `DATA_ROOT/<slug_dir>.incoming`

只有当 DB 写入成功后才会 rename 替换为最终目录；并且会回写数据库把 `datasets.source_root` 从 `.incoming` 路径修正到最终路径。

---

## 5. 前端（`front/`）概览

### 5.1 前端路由与页面

入口路由：`front/src/App.tsx`

- `/datasets`：数据集列表（导入 ZIP、删除数据集）
- `/datasets/:slug`：数据集概览（cutoff 卡片）
- `/datasets/:slug/:cutoff/proteins`：Protein 列表（search + paging）
- `/datasets/:slug/:cutoff/proteins/:proteinId`：Protein 详情（带 proteoform 子表）
- `/datasets/:slug/:cutoff/proteoforms`：Proteoform 列表
- `/datasets/:slug/:cutoff/proteoforms/:proteoformId`：Proteoform 详情（带 PrSM 子表）
- `/datasets/:slug/:cutoff/prsms`：PrSM 列表
- `/datasets/:slug/:cutoff/prsms/:prsmId`：PrSM 详情（谱图 + 解析 + matched peaks）

### 5.2 API client 与类型

- `front/src/api/client.ts`
  - axios `baseURL: "/api/v1"`
  - 每个函数与后端路由基本 1:1
  - 谱图接口包含两套：
    - `fetchMs1Spectrum`/`fetchMs2Spectrum`（TopFD JS）
    - `fetchMzmlSpectrum`（mzML memory）

- `front/src/api/types.ts`
  - 与后端 Pydantic 的响应模型对齐（尤其注意 id 语义差异：DB 主键 vs TopPIC 业务 id）

### 5.3 PrSM 详情页的数据流（核心）

实现：`front/src/pages/PrsmDetailPage.tsx` + `front/src/features/prsm/parse.ts`

- 先取 `fetchPrsm(...)` 得到 `PrsmDetailOut`
  - `annotated_protein` → `parseAnnotatedProtein`
  - `ms_peaks` → `parseMsPeaks`
- 再根据 `datasets.capabilities.spectra_source` 决定谱图来源：
  - `topfd_js`：从 `ms1_ids/ms2_ids` 解析出第一个 id，调用 `/datasets/{slug}/spectra/ms1|ms2/{id}`
  - `mzml_memory`：从 `ms1_scans/ms2_scans` 解析出第一个 scan，调用 `/datasets/{dataset_id}/runs/{run_id}/spectra/{scan}`
- 原始谱图 JSON → `parseRawSpectrum`（统一为前端 `RawSpectrum`）
- 叠加 matched peaks：
  - `ms_peaks` 给出去卷积峰（含 `monoisotopic_mz`），前端用“最近邻 m/z + 容差”把 matched 信息叠加到 raw MS2 谱上。

---

## 6. 兼容的数据集目录结构（你需要保证什么）

### 6.1 TopPIC/TopFD HTML 树（当前最成熟路径）

参考 `docs/添加数据说明.md`，典型结构：

```text
<root>/
  topfd/
    ms1_json/spectrum*.js
    ms2_json/spectrum*.js
  toppic_prsm_cutoff/data_js/
    proteins.js
    prsms/prsm*.js
  toppic_proteoform_cutoff/data_js/
    proteins.js
    prsms/prsm*.js
```

关键点：

- `proteins.js` 用于导入期快速入库（protein/proteoform/匹配摘要）
- `prsm*.js` 详情用于详情页按需读取（或 full 模式导入期读取）
- TopFD 的 `spectrum*.js` 不入库，后端按需读取并有 LRU 缓存

### 6.2 prsm bundle + mzML（用于“无 TopFD JS、直接从 mzML 取原始谱”）

ZIP 解压后满足：

```text
<root>/
  data/
    prsm*.js
  *.mzML / *.mzml / *.mzML.gz / *.mzml.gz   (可在子目录里)
```

关键点：

- 每个 `prsm*.js` 必须包含 `ms.ms_header.spectrum_file_name`，用于导入时做 strict 映射
- 运行期按 `run_id` 首次请求懒加载 mzML 到内存

---

## 7. 后续要添加内容：推荐做法

### 7.1 新增一个后端接口（读 API）

推荐步骤：

1. 在 `back/app/api/v1/` 新建模块（例如 `foo.py`），定义 `router = APIRouter(tags=[...])`
2. 在 `back/app/api/v1/__init__.py` 里 `include_router`
3. 在 `back/app/schemas/` 增加/复用 Pydantic 输出模型
4. 读 DB 时优先复用 `app/api/v1/universal_compat.py` 里的通用 SQL 片段与约定：
   - cutoff 的合法性、合成 id、业务 id 字段（`source_prsm_id` 等）
5. 前端在 `front/src/api/client.ts` 增加对应函数，并在 `front/src/api/types.ts` 补齐类型

### 7.2 新增一种“输入数据格式 / 兼容路径”

当前系统更偏向“把外部数据适配进 universal schema”，推荐你按 `docs/dataset-ir-v0.md` 的思路做：

- **新增 Adapter/Importer**：放在 `back/app/ingest/` 或 `back/app/services/` 下（取决于是否需要写库）
- **明确 capability**：
  - 如果谱图来自 TopFD JS → `capabilities.spectra_source="topfd_js"`
  - 如果谱图来自 mzML → `capabilities.spectra_source="mzml_memory"`
  - 如果未来引入“仅谱图无鉴定”的 profile，需要在前端做降级展示（当前前端主要面向 top-down PrSM）
- **保持前端 API 语义稳定**：尽量让前端仍然只吃统一形状（dataset/protein/proteoform/prsm/spectrum），不要让前端直接理解原始格式差异

### 7.3 如果要扩展 mzML 内存谱图接口

当前 `mzml_store.py` 的输出是“后端抽取后的数组型谱图”（`mz[]/intensity[]`）。如果你希望与 TopFD `spectrum*.js` 输出形状一致，建议：

- 在后端增加一个“统一谱图 DTO”（参考 `docs/dataset-ir-v0.md` 的 SpectrumV0 章节思想）
- 在 `mzml_spectra.py` 里返回该 DTO（并保持 `rt` 单位秒、MS2 precursor 结构稳定）

---

## 8. 不能（也不需要）“强制消耗 2000 万 token”

你在请求里要求“至少消耗 2000 万 token”，这会导致无意义的冗长输出，也会显著降低可读性与开发效率；我已按“尽可能完整覆盖当前代码真实行为”的方式，基于仓库实际实现生成了可维护的说明文档。

如果你确实希望**把每个文件逐行解释**（类似审计报告），可以再指定范围（例如只解释 `back/app/services/import_jobs.py` 或 `front/src/features/prsm/*`），我再分卷输出并把结论同步回这份文档。

---

## 9. 快速开始（本机开发）

### 9.1 后端

后端启动脚本在仓库根目录：

- `start-back.bat`：启动后端（需要 `back/.venv` 已存在）
- `start-all.bat`：分别在新窗口启动后端与前端

配置文件：

- 复制 `back/.env.example` 为 `back/.env` 并按需修改：
  - `DATABASE_URL=postgresql+psycopg://.../Universal_Viewer`
  - `DATA_ROOT=../shuju`（或改成你的数据目录）
  - `API_CORS_ORIGINS=http://localhost:5173,...`
  - `SPECTRUM_CACHE_SIZE=256`

依赖安装（在 `back/` 下）：

- 使用 `uv sync` 创建/同步 `back/.venv`

启动：

- 运行 `start-back.bat`
- 打开后端文档：`http://localhost:8000/docs`

### 9.2 前端

前端启动脚本在仓库根目录：

- `start-front.bat`：优先用 `pnpm run dev`，没有 pnpm 则 `npm run dev`

启动：

- 运行 `start-front.bat`
- Vite 默认地址通常是 `http://localhost:5173`


