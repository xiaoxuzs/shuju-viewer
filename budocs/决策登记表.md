# Bottom-Up DIA 统一决策登记表

> **文档版本**：v1.3  
> **日期**：2026-05-21  
> **状态**：已定稿（实施前须全部为 ✅）  
> **关联**：[Viewer接入规划-完整版.md §16](./Viewer接入规划-完整版.md#16-已确认决策清单)

实施前将下表全部项落实为 ✅；代码与专项文档须与本表一致，矛盾时以本表为准。

### 锁定规则

| 条件 | 行为 |
|---|---|
| 19 项全部为 ✅（当前状态） | 登记表 **锁定**；实施 PR 只引用编号，不 reopen 讨论 |
| 需变更某条决策 | 在本表新增行或升版本（v1.1），标注旧决策 **superseded**，并同步改关联专项文档 |
| 代码与登记表矛盾 | **以本表为准**；修代码或走登记表修订流程，禁止 silent drift |

锁定后验收对照：[验收测试矩阵.md](./验收测试矩阵.md)（导入 / API / 谱图 / Coverage / 前端五域）。

---

## 登记表（19 项）

| # | 决策项 | 定稿 | 影响文档 | 影响代码 |
|---|---|---|---|---|
| D1 | 主报告用 `all_report` 还是 `target_report` | ✅ **all_report**；`target_report` 仅登记路径（`extra_metadata.refined_report_path`） | 导入 §17、[Bottom-Up数据导入规划.md](./Bottom-Up数据导入规划.md) | ingest adapter |
| D2 | Q.Value 导入阈值 | ✅ 导入固定 **0.01**；列表 UI 可再筛（`q_max`） | 导入 §17、前端 §2.5、[BU前端接入规划.md](./BU前端接入规划.md) | lists API |
| D3 | 蛋白组拆分 | ✅ `A;B;C` → **3 条** `protein` + `protein_relation_mapping` | 导入 §17 | ingest |
| D4 | 双 run 默认展示 | ✅ 整体页 **run 下拉**，默认 **mzML** run | 导入 §17、前端 P1 | overview |
| D5 | ingest 根必须含谱图 | ✅ v1 **不允许** parquet 与谱图分盘 | 导入 §17 | resolver |
| D6 | 后端包名 | ✅ 统一 **`back/app/bu/`**（不用 `bottom_up`） | [谱图查看说明.md](./谱图查看说明.md) §2 | 全部 BU 后端 |
| D7 | mixed 预载策略 | ✅ v1：仅 **mzML run** 走 `spectrum_memory`；**.d** 走 `tdf_reader` | [BU运行时后端模块规划.md](./BU运行时后端模块规划.md) §15 | wiring + capabilities |
| D8 | BU 列表 API 位置 | ✅ 新建 **`api/v1/bu/*`**，不改 TD 文件 | 运行时 §15 | router |
| D9 | `resolved_scan` 回写 DB | ✅ 写 **`extra_metadata.resolved_scan`** | 运行时 §15 | scan_resolver |
| D10 | v1 MS2/XIC 对 `.d` run | ✅ **M5 仅 mzML**；**M8 补 tdf**；v1 声明不支持 `.d` match 读 MS2 | 运行时 §15 | spectrum_facade |
| D11 | Run–parquet 匹配失败 | ✅ **警告 + 单 run fallback**；**多 run 且无匹配 → 导入失败** | 导入 §8.2、谱图 §9 | run_discovery |
| D12 | BU 数据集 `cutoffs` | ✅ **`cutoffs: []`**（非两条 0 计数占位） | [BU列表与数据集API规范.md](./BU列表与数据集API规范.md) §3.2 | datasets.py |
| D13 | 列表搜索参数名 | ✅ URL/API 统一 **`search`**（不用 `q`） | 前端 §2.5 | buClient |
| D14 | metadata 键名规范 | ✅ 入库 **snake_case**：`pg_max_lfq`, `pg_q_value`, `genes` | 导入 §8.3、前端 §4.1 | field_mapping |
| D15 | Pydantic schema 位置 | ✅ **`back/app/schemas/bu.py`**（API 层）；`bu/services` 只引用 | API §14、运行时 §4 | 目录结构 |
| D16 | TD 访问 `/datasets/:slug` | ✅ **保留 `DatasetPage`**：展示 cutoff 卡片，**不**自动跳转 prsms（与现网 Viewer 一致） | [BU前端接入规划 §9.4](./BU前端接入规划.md#94-datasetmodegate-行为)、P0 C28 | `DatasetPage` + `DatasetModeGate` |
| D17 | 浏览器 URL 与分析模式不一致 | ✅ **软着陆**：按 `analysis_mode` **重定向**到该模式合法入口；**404 仅**用于 slug/实体不存在 | [BU前端接入规划 §7.1 / §9.4](./BU前端接入规划.md#71-页面级守卫) | `DatasetModeGate`、`BuModeOnly` |
| D18 | BU 列表查询索引 | ✅ **必建**：M1 后、**R4 列表 API 前** migration 落地 `(dataset_id, q_value)` 与 `(dataset_id, run_id)`；列表须走 index scan（性能优先） | [导入规划 §18](./Bottom-Up数据导入规划.md#18-数据库索引必建) | migration + P0 C4 |
| D19 | 列表 `decoy` 参数语义 | ✅ **`decoy=false`（默认）排除 decoy；`decoy=true` 包含**（proteins / matches / rt-mz 一致；与前端 §2.5 / §4.1 对齐） | [API §7.1 / §11.1](./BU列表与数据集API规范.md#71-查询参数)、[BU前端接入规划 §2.5 / §4.1](./BU前端接入规划.md) | `lists_service` |

---

## 按域索引

| 域 | 决策项 |
|---|---|
| 导入 / ingest | D1–D5, D11, D14, **D18** |
| 运行时 / 谱图 | D6–D10, D9 |
| API / 前端契约 | D8, D12, D13, D15, **D19** |
| 前端实现 | D4, D13, D16, D17（及 D2 列表筛） |

---

## 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-05-21 | 首版：15 项全部定稿 ✅ |
| v1.0.1 | 2026-05-21 | 增加锁定规则；链至验收测试矩阵 |
| v1.1 | 2026-05-21 | 新增 D16（TD 保留 DatasetPage）、D17（路径软重定向） |
| v1.2 | 2026-05-21 | 新增 D18（BU 列表索引必建，性能优先） |
| v1.3 | 2026-05-21 | 新增 D19（`decoy` 统一为 false 排除、true 包含） |

---

*专项文档 §17 / §15 中的待确认表已迁入本表；实施 PR 描述可引用决策编号（如 D11）。*
