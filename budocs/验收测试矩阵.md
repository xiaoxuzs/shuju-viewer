# Bottom-Up DIA 验收测试矩阵

> **文档版本**：v1.0  
> **日期**：2026-05-21  
> **状态**：实施收尾必跑  
> **关联**：[决策登记表.md](./决策登记表.md)（锁定后对照）、[Viewer接入规划-完整版.md §18.2](./Viewer接入规划-完整版.md#182-推荐阅读顺序)

本文是 **E2E 验收清单**：按域列出可重复执行的检查项与期望结果。每项通过方可视为对应里程碑（M1–M7）完成。

**低耦合验收原则**：TD 回归与 BU 新能力 **分开断言**；BU 用例 **不** 修改 `features/prsm/**`；API 用例 **不** 依赖前端路由实现细节。

**P0 任务映射**：各节检查项对应 [P0-Viewer代码改造规划.md §4–§6](./P0-Viewer代码改造规划.md#4-任务清单-c1c31) 中 C 项与 PR 切片。

---

## 目录

1. [导入（M1）](#51-导入m1)
2. [数据集 API（M3/M4）](#52-数据集-apim3m4)
3. [谱图（M5–M6）](#53-谱图m5m6)
4. [Sequence Coverage（M7）](#54-sequence-coveragem7)
5. [前端（M4–M7）](#55-前端m4m7)

---

## 5.1 导入（M1）

| # | 检查项 | 期望 | 验证方式 |
|---|---|---|---|
| 1 | `POST /imports` + `d:\dia-shuju` | job `status=success` | API / DatasetsPage |
| 2 | `datasets.analysis_mode` | `BOTTOM_UP` | psql 或 `GET /datasets/:slug` |
| 3 | `identification_matches` 行数 | ≈ **110,026**（Q&lt;0.01） | psql count |
| 4 | `runs` 行数 | = mzML 数 + `.d` 数；`.d` 用**内层** tdf 路径 | psql + `file_path` 检查 |
| 5 | 重复导入同 fingerprint | **拒绝**（409 或等价） | 二次 `POST /imports` |
| 6 | TD histone 数据集 | 导入 / 打开 **回归通过** | 手动打开现有 TD slug |
| 7 | 导入进度 stage | 中文 label 与 [导入规划 §12](./Bottom-Up数据导入规划.md#12-导入阶段与进度条) 一致 | 观察 DatasetsPage 进度条 |

---

## 5.2 数据集 API（M3/M4）

| # | 检查项 | 期望 | 验证方式 |
|---|---|---|---|
| 1 | `GET /datasets` | 响应为 **`DatasetOut[]` 数组**；每项含 **`analysis_mode`**、`status` | JSON 断言 |
| 2 | BU 数据集 `GET /datasets/:slug` | **`cutoffs: []`** | JSON 断言 |
| 3 | TD 数据集 | `cutoffs` 仍为 prsm / proteoform | JSON 断言 |
| 4 | `GET /datasets/:slug/overview` | 返回 counts + qc + runs | JSON schema |
| 5 | `GET .../matches?q_max=0.01` | `total` ≈ **110,026** | API total 字段 |
| 6 | 列表索引（**D18 必过**） | `EXPLAIN ANALYZE` 对 `matches?q_max=0.01` 为 **Index/Bitmap Index Scan**，非 Seq Scan；未建 `idx_im_dataset_q` **禁止**上线 R4 | psql（见导入 §18） |

---

## 5.3 谱图（M5–M6）

| # | 检查项 | 期望 | 验证方式 |
|---|---|---|---|
| 1 | LLLPGELAK match MS2 | scan ≈ **67726**，b/y ≥ **10** | `GET .../matches/:id/spectrum/ms2` |
| 2 | 同 match XIC | 点数 ~**424** 量级；含 `rt_start` / `rt_stop` / `apex` | `GET .../xic` |
| 3 | 整体页 TIC | `unit_rt=min`；过长 run 有 **downsampled** | chromatogram API + UI |
| 4 | 整体页 | **不**请求 `/matches/.../spectrum/*` | 浏览器 Network 面板 |
| 5 | `.d` run | `dia-windows` 返回 **200** 条量级 | `GET .../runs/:id/dia-windows` |
| 6 | `.d` run 请求 G5（match MS2） | **404** 或明确 unsupported（v1） | API + [Viewer §17](./Viewer接入规划-完整版.md#17-v1-明确不支持项) |
| 7 | mzML run match MS2 | **200** + 峰列表 | 同上 |

---

## 5.4 Sequence Coverage（M7）

| # | 检查项 | 期望 | 验证方式 |
|---|---|---|---|
| 1 | P62805 蛋白详情 | 有 `base_sequence` + **segments** | `GET .../proteins/:id` |
| 2 | 断网无缓存 | `coverage_mode=list_only` | DevTools offline |
| 3 | Decoy 蛋白 | `coverage_mode=decoy` | decoy accession |
| 4 | 蛋白详情页 Network | **无** MS2 / XIC 请求 | Network 面板 |

---

## 5.5 前端（M4–M7）

| # | 检查项 | 期望 | 验证方式 |
|---|---|---|---|
| 1 | 打开 **TD** 数据集 | 仍走 prsm 路由，**无** BU Tab | 手动 |
| 2 | 打开 **BU** 数据集 | Tab：**Overview / Proteins / Peptides / Matches** | 手动 |
| 3 | 列表 URL | `search` + 筛选参数 **可分享**（刷新保持） | 复制 URL |
| 4 | DatasetsPage | BU 卡片有 **Bottom-Up** badge；TD 有 Top-Down badge | `/datasets` |
| 5 | BU `cutoffs` 空 | 列表 / 壳层 **不**显示 cutoff 相关 UI | 手动 |
| 6 | `features/prsm/**` | `git diff` **为空** | `git diff -- front/src/features/prsm` |
| 7 | 导入 UI | stage 中文与 progress 与 job 对齐 | DatasetsPage 导入一次 |

---

## 修订记录

| 版本 | 日期 | 说明 |
|---|---|---|
| v1.0 | 2026-05-21 | 首版：五域 E2E 矩阵 |

---

*实施 PR 合并前建议在本文件勾选通过项，并在 PR 描述中引用章节号（如「验收 5.3 #1 通过」）。*
